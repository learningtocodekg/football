name: "A4 — LLM WR + LLM QB + LLM CB, corner route"
phase: A4
description: >
  1st and 10. All three agents live. WR runs a corner route — deep upfield stem
  (~2.5s, 12+ yards), then diagonal cut toward the outside corner of the end zone
  at 315° (upfield-left). A deeper version of the out route, designed to beat
  CB pressing tight coverage deep. QB throws to the back corner.
  Tests: deep route coverage, QB accuracy on a corner throw, WR separation at depth.

down: 1
distance: 10
wr_route: corner
expected_open_t: 2.8

a4_mode: true

wr_model: "gpt-5-nano"
wr_reasoning_effort: "low"
wr_provider: "openai"

qb_model: "gpt-5-nano"
qb_reasoning_effort: "low"
qb_provider: "openai"

cb_model: "gpt-5-nano"
cb_reasoning_effort: "low"
cb_provider: "openai"
