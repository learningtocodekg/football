Decide your pursuit intent for THIS 0.1s step. Read the GEOMETRY, the LEVERAGE facts, and the MOVE LOG
(your moves and the WR's, side by side), then reason it through before you commit:

1. WHAT IS HE DOING? Is he still stemming (running upfield, setting you up), or has he committed a REAL
   break — a heading change that HELD, not a one-step jab that snapped back? Is this a foot race (he is
   even with / past you)? The "CUT CONFIRMED" line, if present, means a real cut is already established —
   don't re-litigate it.

2. CAN I STILL HOLD THE CUSHION? Read the LEVERAGE facts: does your backpedal still match his speed, and
   is your shade balanced? If `shadow` still holds him, stay in it with your hips open. If the facts say
   he's faster than your backpedal, `shadow` is already losing.

3. COMMIT OR SHADE? Flipping to `drive`/`bail` is a hip commitment with a real recovery cost (shown) —
   spend it only on a real break or a foot race you're about to lose, then STAY with it. To merely adjust
   leverage, use a small `tilt` instead — that's free. Don't churn modes on a fake.

Output JSON:
{"mode": "shadow"|"drive"|"bail", "tilt": <deg, -25..25, - = outside / + = inside>, "reasoning": "<short>"}
