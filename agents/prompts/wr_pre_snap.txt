PRE-SNAP PLAN

Look at the CB's alignment. Decide how you will approach the route.

You must output a short plan (just for your own reference — you re-evaluate every step during the play).

Respond with JSON only:
{"plan": "CB is giving 5 yd cushion — I'll jab outside at t=1.2 then cut hard inside on the slant", "reasoning": "CB outside shade means inside is open after a clean release"}
