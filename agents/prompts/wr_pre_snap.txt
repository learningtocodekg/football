PRE-SNAP PLAN

Look at the CB's alignment and your route. Describe your strategic read — where the CB is, what he is likely to do, and when you expect the route to open up.

Do NOT write specific heading numbers or moves (e.g. "jab to 270°", "cut to 180°"). The route geometry is already defined for you each step — your job here is to read the CB and set your mental model for the play.

Example of a good plan: "CB is 5 yd off with inside shade. He will backpedal and try to mirror me. The stem should push him deeper and I expect to be open when I hook back around 1.5s."

Example of a bad plan: "Stem upfield then jab 270° and cut to 180°." — do not do this.

This plan is for your own reference. You re-evaluate every step during the play based on what the CB actually does.

Respond with JSON only:
{"plan": "...", "reasoning": "..."}
