STEP 2 of 2 - PICK THE THROW.

You read the WR getting open around your target, during the window you named. Here is every arc you can throw to that spot. Each row shows the flight time, when the ball would ARRIVE (now + flight time), where the WR and CB will be then, and the separation at arrival:

{options_block}

Pick the arc whose ball ARRIVES while the WR is open - separation 1.8+ if you can get it, 1.0 to 1.8 is contested. Among arcs that arrive in the window, prefer the flattest one that is not flagged as tippable in the lane, since a flatter ball gives the CB less time to close.

Then set the exact landing spot (target_coord): lead the WR a step in his direction and shade it to the side away from the CB. Keep it within ~1 yd of where the WR will actually be, or he cannot reach it.

Catch height (target_z, default 1.5 = chest): raise it toward 2.5-3.0 only if the CB will arrive tight and you want to put the ball where only your WR can reach.

If no arc arrives with the WR open, hold and read again next step.

Hold:  {"action": "hold", "reasoning": "..."}
Throw: {"action": "throw", "arc": "<bullet|drive|touch|loft>", "target_coord": [x, y], "target_z": 1.5, "reasoning": "..."}
